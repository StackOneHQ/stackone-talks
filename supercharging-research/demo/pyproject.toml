[project]
name = "research-agent-demo"
version = "0.1.0"
description = "Demo: Supercharging Research with Agents"
requires-python = ">=3.11"
dependencies = [
    "anthropic",
    "playwright",
    "arxiv",
    "pyyaml",
]

[tool.uv]
dev-dependencies = []
