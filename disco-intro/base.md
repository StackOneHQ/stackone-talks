  1. Title — "Disco" hero text, your name/role
  2. About Me — PhD Bristol, ML @ StackOne
  3. StackOne — integration infra, 200+ connectors, MCP native
  4. What is a tool — terminal showing a tool JSON definition
  5. "Context suicide" — section label
  6. Tool Bloat — animated context bar filling to 74% red + token counts
  8. DEMO — blow the context live
  9. Performance — "I want everything available and fast"
  10. "Building Tool Search" — section label
  11. Search flow — animated pipeline: query → search → top-k → call
  12. Search methods — BM25 / semantic / LLM boxes
  13. We need to measure — animated pipeline: benchmark → compare → pick
  14. Competitors — [PLACEHOLDER]
  15. Mock connectors — terminal showing mock setup
  16. Dataset — terminal with query→tool examples
  17. Baseline results — comparison table [PLACEHOLDER numbers]
  18. Fine-tuning — training data + Modal boxes
  19. Fine-tuned results — animated embedding scatter + table [PLACEHOLDER]
  20. DEMO — tool search working
  21. "Sick it works...until the response" — pivot text
  22. "Response Bloat" — section label
  23. Responses are massive — animated terminal flooding with JSON + "CONTEXT BLOWN"
  24. Code Mode diagram — Guillaume's full architecture: Agent → search/write → sandbox (tsx) with MCP client, real API
  calls, filtering
  25. Truncation iterations — side-by-side: raw JSON (bar fills red 95%) vs dynamic summary (green 18%)
  26. Benchmarking code mode — [PLACEHOLDER]
  27. Results — token savings + accuracy [PLACEHOLDER]
  28. "Shipping it" — section label
  29. Infrastructure — Cloudflare Workers, cred injection, no downloads
  30. DEMO — Disco live end-to-end
  31. Access — alpha, self-serve [PLACEHOLDER]
  32. Thanks — QR codes, social links
